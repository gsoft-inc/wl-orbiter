import { Canvas, ComponentInfo, Link, Tagline } from "@stories/components";
import { Meta } from "@storybook/addon-docs";
import * as AsideStories from "./aside.stories.tsx";

<Meta of={AsideStories} />

# Aside

<Tagline>A specialized <Link href="?path=/docs/box--default-story">box</Link> component for HTML aside element.</Tagline>

<ComponentInfo
    usage={"import { Aside } from \"@workleap/orbiter-ui\";"}
    githubPath="/packages/components/src/html/src"
/>

## Usage

An aside component accepts all the [aside HTML element attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/aside) and [Orbiter styled component props](?path=/docs/styling--docs).

<Canvas of={AsideStories.Example} />
