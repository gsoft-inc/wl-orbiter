import { ComponentInfo, Link, Tagline } from "@stories/components";
import { Canvas, Meta } from "@storybook/addon-docs";
import * as AddressStories from "./address.stories.tsx";

<Meta of={AddressStories} />

# Address

<Tagline>A specialized <Link href="?path=/docs/box--default-story">box</Link> component for HTML address element.</Tagline>

<ComponentInfo
    usage={"import { Address } from \"@workleap/orbiter-ui\";"}
    githubPath="/packages/components/src/html/src"
/>

## Usage

An address component accepts all the [address HTML element attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/address) and [Orbiter styled component props](?path=/docs/styling--docs).

<Canvas of={AddressStories.Example} />
