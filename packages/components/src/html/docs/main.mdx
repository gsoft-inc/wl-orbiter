import { ComponentInfo, Link, Tagline } from "@stories/components";
import { Canvas, Meta } from "@storybook/addon-docs";
import * as MainStories from "./main.stories.tsx";

<Meta of={MainStories} />

# Main

<Tagline>A specialized <Link href="?path=/docs/box--default-story">box</Link> component for HTML main element.</Tagline>

<ComponentInfo
    usage={"import { Main } from \"@workleap/orbiter-ui\";"}
    githubPath="/packages/components/src/html/src"
/>

## Usage

A main component accepts all the [main HTML element attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/main) and [Orbiter styled component props](?path=/docs/styling--page).

<Canvas of={MainStories.Example} />
