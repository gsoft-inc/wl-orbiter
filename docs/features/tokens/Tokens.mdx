import { Meta, Source, Story } from "@storybook/addon-docs";
import {
    TextColors,
    BackgroundColors,
    BorderColors,
    IconColors,
    Shadows,
    FontFamily,
    FontSizes,
    FontWeight,
    LineHeight,
    Radii,
    ComplexPadding,
    SimplePadding,
    SimpleMargin,
    Sizing
    } from "./Tokens";
import { TokenTable } from "./TokenTable";
import * as TokensStories from "./Tokens.stories";

<Meta of={TokensStories} />

# Tokens

Tokens are Orbiter's reusable values to provide a common language between developers and designers.

> If you are looking for a list of available CSS tokens, please refer to the [Hopper tokens page](https://hopper.workleap.design/tokens/getting-started/introduction).

## Usage

Tokens can be used as [style props](/docs/style-props--docs) values:

<Source dark language="tsx" of={TokensStories.Usage} />

Or `CSS` variables:

<Source dark language="css" code={`
.item {
    background-color: var(--hop-neutral-surface-weak);
}
`} />

## Aliased tokens

Those tokens are named for their use case, rather than their value. For example `danger` is named to indicate the appropriate background color for an error message background, as opposed to `amanita-400` which is less specific and could be used in a number of ways depending on the context.

This makes their intended use clear and intentional, and allows us to develop a scalable and consistent visual language while also being the building block of Orbit's [theming](/docs/theming--docs).

### Deprecation notice

Tokens with a `-active` suffix are deprecated and should not be used in new designs. They will be removed in a future release, prefer using their `-press` counterparts. In some instances `-active` tokens should be replaced with a `-selected` token, this should be documented in your Figma files, in doubt validate with a designer.

## Values

### Background Colors

<TokenTable
    tokens={BackgroundColors.Semantic}
/>

### Border Colors

<TokenTable
    tokens={BorderColors.Semantic}
/>

### Box Shadows

<TokenTable
    tokens={Shadows.Semantic}
/>

### Icon Colors

<TokenTable
    tokens={IconColors.Semantic}
/>

### Font Family

Prefer using the [Text](/docs/text--size) or [Heading](/docs/heading--default-story#size) components instead of using the font family tokens directly.

<TokenTable
    tokens={FontFamily.Semantic}
/>

### Font Sizes

Prefer using the [Text](/docs/text--size) or [Heading](/docs/heading--default-story#size) components instead of using the font size tokens directly.

<TokenTable
    tokens={FontSizes.Semantic}
/>

### Font Weights

Prefer using the [Text](/docs/text--size) or [Heading](/docs/heading--default-story#size) components instead of using the font weight tokens directly.

<TokenTable
    tokens={FontWeight.Semantic}
/>

### Line Heights

Prefer using the [Text](/docs/text--size) or [Heading](/docs/heading--default-story#size) components instead of using the line height tokens directly.

<TokenTable
    tokens={LineHeight.Semantic}
/>

### Radii

<TokenTable
    tokens={Radii.Semantic}
/>

### Sizings

<TokenTable
    tokens={Sizing.Core}
/>

### Spacings

#### Complex Padding (only in padding:)

<TokenTable
    tokens={ComplexPadding.Semantic}
/>

#### Any Padding (padding:, paddingLeft:, paddingX:, paddingBottom:, ...)

<TokenTable
    tokens={SimplePadding.Semantic}
/>

#### Margin

<TokenTable
    tokens={SimpleMargin.Semantic}
/>

### Text Colors

<TokenTable
    tokens={TextColors.Semantic}
/>
